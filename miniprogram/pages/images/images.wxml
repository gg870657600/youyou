<view style="flex-direction:column;">
  <block wx:for="{{imageId}}" wx:key="{{index}}"> 
    <image src="{{item}}" mode="widthFix"  data-src="{{item}}" data-list= "{{imageId}}" bindtap='imgClick'></image>
  </block>
</view>